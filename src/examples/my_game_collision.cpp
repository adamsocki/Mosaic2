
void TestMouseCollision() {
	
	/*
	vec2 mousePos = Input->mousePosNormSigned;
	mousePos.x = mousePos.x * 8;
	mousePos.y = mousePos.y * 4.5f;
	DrawSprite(mousePos, V2(0.5f, 0.5f), &Data->lemonSprite);

	Circle circlePlayer;
	Circle circleMouse;
	circlePlayer.center = p->position;
	circlePlayer.radius = 1;

	circleMouse.center = mousePos;
	circleMouse.radius = 1;
	// Step 2: Measure Distance between each vector
	real32 distanceCalculation = Distance(mousePos, p->position);
	DrawTextScreenPixel(&Game->monoFont, V2(540, 251), 13.0f, RGB(1.0f, 1.0f, 1.0f), "%f", distanceCalculation);
	if (distanceCalculation <= circleMouse.radius + circlePlayer.radius) {
		DrawTextScreenPixel(&Game->monoFont, V2(540, 281), 13.0f, RGB(1.0f, 1.0f, 1.0f), "hit");
	}
	*/
}


void CollisionDetection() {

	TestMouseCollision();



}

